{
  "name": "vulktek-frontend",
  "version": "1.0.0",
  "description": "Static launch page for Vulktek Cybersecurity.",
  "private": true,
  "scripts": {
    "postinstall": "node build-if-workers-ci.js",
    "build:css": "tailwindcss -i input.css -o style.css",
    "build": "npm run build:css && node build.js",
    "deploy": "npm run build && wrangler deploy",
    "deploy:preview": "npm run build && wrangler versions upload"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "@tailwindcss/container-queries": "^0.1.1",
    "@tailwindcss/forms": "^0.5.11",
    "tailwindcss": "^3.4.19",
    "wrangler": "^4.105.0"
  }
}
